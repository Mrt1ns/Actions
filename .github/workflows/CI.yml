name: First Workflow CI
on: workflow_dispatch
jobs:
  New-Job:
    runs-on: ubuntu-latest
    steps:
      - name: Step-One
        run: | 
          echo "Meu primeiro passo no Github Actions"
          echo "Desenvolvido por Vinicius"