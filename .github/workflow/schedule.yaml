name: Workflow

on: 
  schedule:
  - cron: '*/5 * * * *'

jobs:
  test_schedule:
    runs-on: Ubuntu-latest
    steps:
      - name: Echo current time
        run: echo "The currante server time is $(date)"
